// import { SignupContainer } from "features/Authentication";

import { SignupContainer } from "features";

// import { SignupContainer } from "features/Authentication";

const SIgnUp = () => {
  return (
    <div>
      <SignupContainer />
    </div>
  );
};

export default SIgnUp;
